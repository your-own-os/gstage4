Follow existing code style when editing files, including code addition and modification.
 - especially keeping the existing line wrap style.
 - when modifying code, don't use edit tool which auto formats the code, generate patch and patch the file.
